# أمثلة الاستخدام
# Usage Examples

<div dir="rtl">

## مثال كامل: استخراج وإعادة الحواشي

### السيناريو
لديك بحث أكاديمي باللغة العربية يحتوي على 50 حاشية سفلية، وتريد تدقيقه في ChatGPT.

---

## الخطوة 1: الوضع الأصلي

### محتوى الملف: `research.docx`

```
مقدمة البحث
===========

إن العلم في الإسلام له مكانة عظيمة¹، وقد حث الإسلام على طلب العلم²،
وجعله فريضة على كل مسلم ومسلمة³.

قال الإمام أحمد⁴: "العلم لا يعدله شيء لمن صحت نيته".

الحواشي:
---------
¹ انظر: فضل العلم، ابن عبد البر، ص 23
² حديث: "طلب العلم فريضة على كل مسلم" رواه ابن ماجه
³ المصدر السابق، ص 45
⁴ أحمد بن حنبل (ت. 241هـ)، من أعلام الحديث
```

---

## الخطوة 2: استخراج الحواشي

### استخدام Python:

```bash
cd word_footnotes_tools/python
python3 extract_footnotes.py ~/research.docx ~/research_extracted.docx
```

### أو استخدام VBA:
1. افتح `research.docx`
2. Alt + F11
3. أدرج ماكرو `ExtractFootnotes.bas`
4. شغل الماكرو
5. احفظ باسم `research_extracted.docx`

### النتيجة: `research_extracted.docx`

```
مقدمة البحث
===========

إن العلم في الإسلام له مكانة عظيمة &&انظر: فضل العلم، ابن عبد البر، ص 23&&،
وقد حث الإسلام على طلب العلم &&حديث: "طلب العلم فريضة على كل مسلم" رواه ابن ماجه&&،
وجعله فريضة على كل مسلم ومسلمة &&المصدر السابق، ص 45&&.

قال الإمام أحمد &&أحمد بن حنبل (ت. 241هـ)، من أعلام الحديث&&: "العلم لا يعدله شيء لمن صحت نيته".
```

---

## الخطوة 3: التدقيق في ChatGPT

### نسخ النص والطلب من ChatGPT:

```
الرجاء تدقيق النص التالي لغوياً ونحوياً مع الحفاظ على النص بين &&:

[لصق النص هنا]
```

### نص ChatGPT المدقق:

```
مقدمة البحث
===========

إن للعلم في الإسلام مكانة عظيمة &&انظر: فضل العلم، ابن عبد البر، ص 23&&،
وقد حث الإسلام على طلبه &&حديث: "طلب العلم فريضة على كل مسلم"، رواه ابن ماجه&&،
وجعله فريضة على كل مسلم ومسلمة &&المصدر السابق، ص 45&&.

قال الإمام أحمد &&أحمد بن حنبل (توفي 241هـ)، من أعلام الحديث&&: "العلم لا يعدله شيء لمن صحت نيته".
```

ملاحظة التغييرات:
- "للعلم في الإسلام" بدلاً من "العلم في الإسلام له"
- "طلبه" بدلاً من "طلب العلم"
- "توفي" بدلاً من "ت."
- إضافة الفاصلة بعد "رواه ابن ماجه"

---

## الخطوة 4: إعادة الحواشي

### انسخ النص المدقق إلى Word

1. افتح Word جديد
2. الصق النص المدقق
3. احفظ باسم `research_corrected.docx`

### استخدام Python:

```bash
python3 restore_footnotes.py ~/research_corrected.docx ~/research_final.docx
```

### أو استخدام VBA:
1. في الملف `research_corrected.docx`
2. Alt + F11
3. أدرج ماكرو `RestoreFootnotes.bas`
4. شغل الماكرو
5. احفظ باسم `research_final.docx`

### النتيجة: `research_final.docx`

```
مقدمة البحث
===========

إن للعلم في الإسلام مكانة عظيمة¹،
وقد حث الإسلام على طلبه²،
وجعله فريضة على كل مسلم ومسلمة³.

قال الإمام أحمد⁴: "العلم لا يعدله شيء لمن صحت نيته".

الحواشي:
---------
¹ انظر: فضل العلم، ابن عبد البر، ص 23
² حديث: "طلب العلم فريضة على كل مسلم"، رواه ابن ماجه
³ المصدر السابق، ص 45
⁴ أحمد بن حنبل (توفي 241هـ)، من أعلام الحديث
```

---

## مثال 2: معالجة دفعية (Batch Processing)

### معالجة عدة ملفات باستخدام Python

```bash
#!/bin/bash
# سكربت لمعالجة عدة ملفات

# استخراج جميع ملفات Word في مجلد
for file in *.docx; do
    echo "معالجة: $file"
    python3 extract_footnotes.py "$file" "${file%.docx}_extracted.docx"
done

echo "تم استخراج جميع الملفات!"
```

---

## مثال 3: دمج مع API الذكاء الصناعي

### سكربت Python كامل للتدقيق التلقائي

```python
#!/usr/bin/env python3
"""
سكربت كامل: استخراج → تدقيق → إعادة
"""

import sys
from extract_footnotes import FootnoteExtractor
from restore_footnotes import FootnoteRestorer
import openai  # يحتاج تثبيت: pip install openai

def proofread_with_ai(text):
    """تدقيق النص باستخدام OpenAI API"""
    client = openai.OpenAI(api_key="your-api-key")

    response = client.chat.completions.create(
        model="gpt-4",
        messages=[{
            "role": "user",
            "content": f"دقق النص التالي لغوياً مع الحفاظ على النص بين &&:\n\n{text}"
        }]
    )

    return response.choices[0].message.content

def main(input_file):
    # 1. استخراج الحواشي
    print("1. استخراج الحواشي...")
    extractor = FootnoteExtractor(input_file)
    extracted_file = extractor.extract()

    # 2. قراءة النص
    print("2. قراءة النص...")
    # هنا تقرأ النص من الملف المستخرج

    # 3. التدقيق
    print("3. التدقيق بواسطة AI...")
    corrected_text = proofread_with_ai(text)

    # 4. كتابة النص المدقق
    print("4. حفظ النص المدقق...")
    # هنا تكتب النص إلى ملف جديد

    # 5. إعادة الحواشي
    print("5. إعادة الحواشي...")
    restorer = FootnoteRestorer(corrected_file)
    final_file = restorer.restore()

    print(f"\n✓ تم! الملف النهائي: {final_file}")

if __name__ == "__main__":
    main(sys.argv[1])
```

---

## مثال 4: حالات خاصة

### حالة 1: حاشية تحتوي على علامة &&

إذا كانت الحاشية نفسها تحتوي على &&:

**الحل:** استخدم escape character:

```python
# في extract_footnotes.py، غير:
text_elem.text = f" &&{footnote_text}&& "

# إلى:
footnote_text_escaped = footnote_text.replace("&&", "&amp;&amp;")
text_elem.text = f" &&{footnote_text_escaped}&& "
```

### حالة 2: حواشي متداخلة

```
نص &&حاشية أولى&& نص آخر &&حاشية ثانية&& النهاية
```

الأدوات تدعم هذا افتراضياً! ✓

---

## نصائح إضافية

### 1. التحقق من عدد الحواشي

قبل وبعد العملية، تحقق من عدد الحواشي:

**Python:**
```python
extractor = FootnoteExtractor("input.docx")
extractor.extract()
# سيطبع: عدد الحواشي السفلية: X
```

**VBA:**
في Word، قبل التشغيل:
```vba
Sub CountFootnotes()
    MsgBox "عدد الحواشي: " & ActiveDocument.Footnotes.Count
End Sub
```

### 2. النسخ الاحتياطي التلقائي

أضف إلى بداية السكربت:

```python
import shutil
from datetime import datetime

# نسخة احتياطية
backup_name = f"backup_{datetime.now().strftime('%Y%m%d_%H%M%S')}.docx"
shutil.copy(input_file, backup_name)
print(f"✓ تم إنشاء نسخة احتياطية: {backup_name}")
```

---

## الأسئلة الشائعة

### س: هل يمكن استخدام الأدوات مع ملفات .doc القديمة؟
ج: لا، يجب تحويلها أولاً إلى .docx

### س: ماذا لو كان النص طويلاً جداً لـ ChatGPT؟
ج: قسم النص إلى أجزاء أصغر، أو استخدم Claude الذي يدعم نصوص أطول

### س: هل تحافظ الأدوات على التنسيق؟
ج: نعم، التنسيق العام يتم الحفاظ عليه، لكن تنسيق الحواشي نفسها قد يختلف قليلاً

---

</div>

# English Examples

## Full Example: Extract and Restore Footnotes

### Step 1: Original Document
```
This is text with a footnote¹.

Footnotes:
¹ This is the footnote content
```

### Step 2: After Extraction
```
This is text with a footnote &&This is the footnote content&&.
```

### Step 3: After AI Proofreading and Restoration
```
This is text with a footnote¹.

Footnotes:
¹ This is the corrected footnote content
```

---

**Happy proofreading! 🎉**
